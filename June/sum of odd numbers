let n=parseInt(prompt("Enter a number: "));
function primenum(n)
{
let sum=0; count = 1;
    for (let i = 0; i < n; i++)
    {
        sum += count;
        count += 2;
    }
    return sum;
}

console.log(primenum(n));
